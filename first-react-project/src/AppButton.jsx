import React from 'react'

// PascalCase: every word starts with uppercase (AppButton, not app_button)
const AppButton = () => {
  return (
    <button className='bg-blue-500 text-white p-2 rounded-md'>Click me</button>
  )
}

export default AppButton
